version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: carpool-redis
    ports:
      - "6379:6379"
    networks:
      - carpool-network

networks:
  carpool-network:
    driver: bridge
